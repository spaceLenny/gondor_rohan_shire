# Core
spacy>=3.7.0
anthropic>=0.30.0

# HuggingFace integration (optional — only needed for TolkienizerTokenizer)
transformers>=4.40.0
torch>=2.0.0

# spaCy English model — install separately:
#   python -m spacy download en_core_web_sm
